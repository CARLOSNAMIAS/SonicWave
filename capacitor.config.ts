import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sonicwave.ai.radio',
  appName: 'SonicWave AI Radio',
  webDir: 'dist'
};

export default config;
